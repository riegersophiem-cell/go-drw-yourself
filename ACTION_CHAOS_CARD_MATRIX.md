# ACTION_CHAOS_CARD_MATRIX

All 16 cards from the ACTION_CHAOS_LIFECYCLE spec. `STATUS` reflects the **engine/rule** layer, verified by automated tests (`tests/rulesEngine.test.ts`, `tests/gameEventDerivation.test.ts`) unless noted — see ACTION_CHAOS_LIFECYCLE_REPORT.md for what on the **presentation** side is confirmed vs. still open.

| CARD | FAMILY | LIFECYCLE | PRESENTATION | CHOICE | TARGET RULE | COLOR RULE | STACK RULE | PERSISTENT STATE | CONSUME CONDITION | STATUS |
|---|---|---|---|---|---|---|---|---|---|---|
| TIMEOUT (`TARGET_SKIP`) | ACTION | PERSISTENT | STANDARD | target player | actor picks any other active player, repeatable | own printed color | n/a | `pendingSkipTargets[playerId]` count, keyed by player not hand | consumed only when that player is the actual landing destination of a turn-advance (walked-past during a SKIP/SKIP ALL jump does **not** consume it) | **PASS** — pre-existing, confirmed correct + covered by test ("skips the chosen target's next own turn…", plus new hand-swap/rotate binding tests) |
| LOWEST (`GIVE_TWO_TO_LOWEST`) | ACTION | IMMEDIATE | STANDARD | none | all active players compared by hand size | own printed color | n/a | none | resolves fully at play time | **FIXED** — was: sole arbitrary player always +2, ties silently ignored. Now: 1 lowest → +2, 2+ tied → +1 each. New tests: 2-way and 3-way tie |
| ONE MORE (`DISCARD_ONE_EXTRA`) | ACTION | PERSISTENT/TEMPORARY | STANDARD | actor's own extra card (free choice) | n/a | own printed color | n/a | `pendingExtraDiscardPlayerId` + phase `WAITING_FOR_EXTRA_DISCARD` | resolved by `DISCARD_EXTRA_CARD`, never re-enters `playCard`/effect resolution | **PASS** — pre-existing; confirmed the extra card never triggers an effect, can't be refused (turn is stuck in this phase until resolved), stays under the action card on the pile |
| DRAW 2 | ACTION | BRANCHING | STANDARD | none (topping is automatic per legality) | next active player, or extends to whoever tops it | own printed color | topper needs `drawAmountOf(card) >= lastDrawValue` (not total, not same-type) | `pendingEffect.{amount,lastDrawValue,terminal:false}` | `DRAW_STACK_RESOLVED` (someone draws) | **FIXED** — was: only another exact `DRAW_2` could top it (Draw 4 illegally blocked). Now value-based per spec. Live-verified: bots chained DRAW_1→DRAW_2→WILD_DRAW_4 correctly, and a DRAW_1 was correctly refused once lastDrawValue was 2 |
| DRAW 4 | ACTION | BRANCHING | STANDARD | none | same as Draw 2 | own printed color | topper needs value `>= lastDrawValue`; a bare Draw 2 is correctly rejected once Draw 4 raised `lastDrawValue` to 4 | same shape as Draw 2 | `DRAW_STACK_RESOLVED` | **FIXED** — same mechanism as Draw 2; dedicated test "forbids Draw 2 from topping an active Draw 4 stack" |
| SKIP | ACTION | IMMEDIATE | STANDARD | none | immediately-next active player | own printed color | n/a | none | resolves at play time | **PASS** — pre-existing; confirmed a TIMEOUT-marked player who is structurally skipped by this card does not lose their mark (test: TARGET_SKIP describe block) |
| REVERSE | ACTION | IMMEDIATE | STANDARD | none | n/a (direction only) | own printed color | n/a | none | resolves at play time | **PASS** — pre-existing; not auto-skip even at 2 players (untouched code path, matches spec) |
| DROP ALL (`DISCARD_ALL`) | ACTION | IMMEDIATE | STANDARD | none (color is the card's own) | n/a | the *played* card's own color decides which cards are dropped — no separate choice | n/a | none | resolves at play time | **PASS** — pre-existing; co-discarded cards never run through `playCard`/effect resolution (architecturally can't trigger), stay under the action card, action card remains top/active color |
| SWAP (`SWAP_HAND`) | CHAOS | IMMEDIATE | MAJOR | color, then target (any other active player) | free choice, self excluded | mandatory choice before target | n/a | none (hand pointer swap only) | resolves at play time | **PASS** — pre-existing; new test confirms a TIMEOUT mark stays bound to the player through the swap, not the hand |
| ROTATE (`ROTATE_HANDS`) | CHAOS | IMMEDIATE/GLOBAL | MAJOR | color only | all active players (human+bot), eliminated excluded | mandatory choice before rotation | n/a | none (hand pointers only) | resolves at play time | **PASS** — pre-existing; new test confirms TIMEOUT survives a rotation |
| SKIP ALL (`SKIP_EVERYONE`) | CHAOS | IMMEDIATE/GLOBAL | MAJOR | color only | every other active player, once each; actor gets an immediate new turn | mandatory choice before resolution | n/a | none (no persistent skip marker created) | resolves at play time | **PASS** — pre-existing; confirmed foreign TIMEOUTs are not consumed by this card's own skip mechanism (same walked-past logic as SKIP) |
| REVERSE +4 (`WILD_REVERSE_DRAW_4`) | CHAOS | TERMINAL | MAJOR | color only | direction flips **first**, then next-in-new-direction becomes final target | mandatory choice | may top an existing normal stack (value-based, same as Draw 2/4); **locks the stack afterward** — nothing may top it | `pendingEffect.terminal = true` | `DRAW_STACK_RESOLVED` | **FIXED (terminal lock)** — direction→target ordering was already correct pre-existing (verified, new regression test added); terminal lock was missing (previously any further draw card could still top it) |
| WILD DRAW 6 | CHAOS | TERMINAL | MAJOR | color only | always the next active player, no free choice | mandatory choice | may top a normal stack; **locks it afterward** | `pendingEffect.terminal = true` | `DRAW_STACK_RESOLVED` | **FIXED (terminal lock)** — target-selection was already correct; terminal lock added. New test plays Draw 2 → Wild Draw 6 → confirms a further Draw 4/Wild Draw 10 is both correctly rejected |
| WILD DRAW 10 | CHAOS | TERMINAL | MAJOR | color only | always next active player | mandatory choice | may top a normal stack; **locks it afterward** | `pendingEffect.terminal = true` | `DRAW_STACK_RESOLVED` | **FIXED (terminal lock)**, same mechanism as Wild Draw 6. Staggered "1–5, beat, 6–10" streaming presentation for 10+ cards — **OPEN**, not built this pass |
| COLOR ROULETTE (`WILD_COLOR_ROULETTE`) | CHAOS | IMMEDIATE/GLOBAL RANDOM | MAJOR | **none** — no player/bot color choice exists for this card | random active player (engine RNG, server-authoritative) | random of the 5 regular colors (engine RNG) | n/a | none | resolves at play time | **FIXED (was a complete gap)** — previously behaved as a reskinned plain WILD: player/bot chose the color themselves, no randomness, no forced direction, target was just "next player". Now: engine picks player+color, forces `direction=1` (clockwise) unconditionally. Client no longer shows a color picker for it. New `CHOSE_COLOR` event emission so the random result is visible to playback. Full 5-phase wheel-spin choreography (spec section 20 Phase A–E) — **OPEN**, not built this pass; currently gets the existing generic MAJOR Action Focus treatment (title, accent color, card motion) |
| WILD WISH (`WILD`) | CHAOS | IMMEDIATE | MAJOR | color only | n/a | actor's free choice | n/a | none | resolves at play time | **PASS** — pre-existing; this is the base/plain Wild card, no other effect, confirmed no side effects beyond `activeColor` |

## Legend
- **PASS** — already matched the spec; verified this pass (test or direct code read), no change needed.
- **FIXED** — genuinely violated the spec before this pass; corrected and covered by a new regression test.
- **OPEN** — a real gap acknowledged and documented per spec section 36, not silently substituted with different behavior.
